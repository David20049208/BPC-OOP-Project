<Window x:Class="InventoryApp.SellDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Prodat položku"
        Height="240" Width="400"
        Icon="resources/favicon.ico"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{StaticResource Bg}"
        TextElement.Foreground="{StaticResource TextColor}"
        TextElement.FontSize="13"
        FontFamily="Segoe UI"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" x:Name="ItemNameTextBlock"
                   FontSize="15" FontWeight="Bold"/>

        <TextBlock Grid.Row="1" x:Name="StockInfoTextBlock"
                   Foreground="{StaticResource Muted}"
                   Margin="0 2 0 16"/>

        <TextBox Grid.Row="2" x:Name="CountTextBox"
                 Style="{StaticResource HintTextBox}"
                 Tag="Počet ks k prodeji"/>

        <TextBlock Grid.Row="3" x:Name="ErrorTextBlock"
                   Foreground="{StaticResource Danger}"
                   FontWeight="SemiBold"
                   Margin="0 8 0 0"/>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Zrušit" Width="90" Margin="0 0 8 0"
                    Style="{StaticResource GhostButton}"
                    IsCancel="True"
                    Click="CancelButton_Click"/>
            <Button Content="Prodat" Width="100"
                    Style="{StaticResource PrimaryButton}"
                    IsDefault="True"
                    Click="ConfirmButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
